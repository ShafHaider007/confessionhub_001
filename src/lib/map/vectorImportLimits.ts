/** Raw GeoJSON / JSON / shapefile ZIP — client parse cap (unchanged). */
export const MAX_VECTOR_IMPORT_BYTES = 15 * 1024 * 1024;

/** User-supplied PMTiles archives — larger OK; still fully client-side. */
export const MAX_PMTILES_IMPORT_BYTES = 500 * 1024 * 1024;
